# DEPRECATED — This file mixed app and backend concerns.
# Use the split env files instead:
#   - apps/app/.env.example    (app-only, safe to commit)
#   - packages/adapters/.env.sample (backend-only, do not commit)
#
# This file is kept for reference only and will be removed in a future release.

# --- Backend-Only Regime Engine Integration Variables ---
# See packages/adapters/.env.sample for full backend configuration.
# DO NOT include these in mobile/app client bundles.
# REGIME_ENGINE_BASE_URL=http://localhost:8787
# REGIME_ENGINE_INTERNAL_TOKEN=your-shared-internal-token

