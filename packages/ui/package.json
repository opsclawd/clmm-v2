{
  "name": "@clmm/ui",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@clmm/application": "workspace:*",
    "react": "18.2.0",
    "react-native": "0.74.0"
  },
  "devDependencies": {
    "@clmm/config": "workspace:*",
    "vitest": "^1.6.0",
    "typescript": "^5.4.0"
  }
}
