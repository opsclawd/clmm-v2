# Dependency Rules

Populated in Sequence 1 implementation. Enforced by dependency-cruiser and ESLint boundary rules.
See `.dependency-cruiser.cjs` and `packages/config/boundaries/dependency-cruiser.cjs`.
