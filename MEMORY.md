# Repo Memory

This file is a router, not the knowledge base.

## How To Use Memory In This Repo

- Before starting new implementation, debugging, or investigation work, check `docs/solutions/` for relevant prior learnings.
- Treat `docs/solutions/` as the source of truth for durable Compound Engineering learnings.
- Use `AGENTS.md` for repo-critical rules that should always be loaded.
- Use architecture and setup docs in `docs/` for stable reference material.

## Where To Look

- `docs/solutions/`: task-specific learnings, prior fixes, failure modes, and reusable patterns
- `docs/compound-engineering.md`: how this repo uses Compound Engineering
- `AGENTS.md`: high-priority agent instructions
- `docs/setup.md`: bootstrap and worktree guidance
- `docs/architecture/`: domain model, repo map, invariants, and sequencing docs

## Maintenance Rule

Do not turn this file into a running log or a duplicate of `docs/solutions/`.
If a learning is durable enough to preserve, store or refresh it in `docs/solutions/` instead.
